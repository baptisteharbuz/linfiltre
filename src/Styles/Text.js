import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    titleContainer: {
        marginTop: 200,
        justifyContent: 'flex-start',
        width: '100%',
        alignItems: 'center',
    },
    title: {
        fontSize: 30,
        marginBottom: 20,
    },
    secret: {
        fontSize: 25,
        marginTop: 20,
        marginBottom: 20
    },
    bold: {
        fontWeight: 'bold',
    },
});